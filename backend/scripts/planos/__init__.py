"""Carga de los planos del taller a la base.

Vive en su propia carpeta porque son varias piezas (Drive, carpeta local, reporte) y
porque el resto de `backend/scripts` son scripts sueltos de una sola corrida.
"""
