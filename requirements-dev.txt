# Dependencias solo para correr los tests (pytest) y los scripts de backend/scripts.
# Instalar con: pip install -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.19
httpx>=0.27

# Google Drive — las usa backend/scripts/planos/importar_planos.py para bajar los
# planos del taller. NO van en requirements.txt: la imagen del backend en Cloud Run no
# habla con Drive, el import se corre a mano desde una máquina.
google-api-python-client>=2.120
google-auth>=2.28
google-auth-oauthlib>=1.2

# Pillow — la usa el importador para achicar las fotos de planos antes de guardarlas.
# En la carpeta del taller hay 80 fotos de celular que se llevan 248 MB; reducidas a
# 2400px pesan una fracción y se abren al toque sobre la conexión del taller. Si no está
# instalada, el importador guarda las fotos tal cual y avisa: no rompe.
Pillow>=10.0
